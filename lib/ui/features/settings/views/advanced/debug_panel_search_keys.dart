/// Localization keys surfaced by the hidden Debug Panel (revealed via
/// long-press on the System Logs AppBar title). Spread into the
/// `system_logs` entry of `kSettingsSearchCatalog` so the panel's fields
/// are discoverable from the settings search once revealed.
const kDebugPanelSearchKeys = <String>[
  'debug_panel',
  'capture_network_and_errors',
  'network_requests',
  'recent_errors',
];
